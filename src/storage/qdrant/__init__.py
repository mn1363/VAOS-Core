"""Qdrant vector storage backend, using the `qdrant-client` package. **Requires a dependency not
yet declared in `pyproject.toml` -- see `driver.py`'s module docstring and
`docs/phase10_summary.md` for exactly what to add.** See `driver.py` for `QdrantVectorStore`,
the sole concrete implementation of `vector.base.VectorStore` in this codebase.
"""
