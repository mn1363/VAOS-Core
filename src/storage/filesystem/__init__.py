"""Filesystem storage backend: one JSON file per entity collection under an injected root
directory, requiring no external service and no third-party dependency beyond the standard
library. See `driver.py` for `FilesystemSourceRepositoryStore`, `FilesystemSourceFileRepository`,
`FilesystemAnalysisRunRepository`, and `FilesystemFindingRepository`.
"""
