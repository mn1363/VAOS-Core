"""Application layer: use cases, DTOs, and orchestration Ports.

The application layer depends only on `domain` and `core`. It defines the
Ports that `infrastructure`, `collectors`, `analyzers`, `extractors` and
`scorers` implement.
"""
